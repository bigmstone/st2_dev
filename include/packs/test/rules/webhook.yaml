---
name: "testwebhook"
pack: "test"
description: "Broken webhook"
enabled: true
trigger:
        type: "core.st2.webhook"
        parameters:
            url: "tests/test1"

action:
    ref: "tests.echoer"
    parameters:
      action_input: 'hello'
